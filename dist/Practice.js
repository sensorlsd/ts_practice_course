class Message {
    constructor(text) {
        this.messageText = text;
    }
    showAlert() {
        alert(this.messageText);
    }
}
let mess = new Message("Testing it's nice");
mess.showAlert();
//# sourceMappingURL=Practice.js.map